services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./app:/root/app
  
  db:
    image: mysql
    ports:
      - "3306:3306"
    volumes:
      - saas_database:/var/lib/mysql
    
    environment:
      - MYSQL_USER=adil
      - MYSQL_PASSWORD=K3ep0u7!!1
      - MYSQL_DATABASE=saas-project
      - MYSQL_RANDOM_ROOT_PASSWORD=1

volumes:
  saas_database: